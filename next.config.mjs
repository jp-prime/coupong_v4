/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export' 설정을 일시적으로 주석 처리하여 SSR 모드로 복귀합니다.
  /*
  output: 'export',
  images: {
    unoptimized: true,
  }
  */
};

export default nextConfig;
